import React from 'react'
import { Hero } from '../../Common/Products/hero-section/Hero'
import { hero } from './contact-us-data'

export const Hero_Section = () => {
  return (
    <Hero hero={hero} page="contact"/>
  )
}
