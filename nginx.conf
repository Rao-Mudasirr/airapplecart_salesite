server {
  listen 80;
  server_name server_name airapplecart.co.uk www.airapplecart.co.uk;

  location / {
    root /usr/share/nginx/html/;
    include /etc/nginx/mime.types;
    try_files $uri $uri/ /index.html;
  }
}